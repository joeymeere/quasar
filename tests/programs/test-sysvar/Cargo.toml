[package]
name = "quasar-test-sysvar"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[lints]
workspace = true

[features]
default = ["alloc"]
alloc = []
debug = []

[dependencies]
quasar-lang = { path = "../../../lang" }

[dev-dependencies]
